import { StructualDirectiveDirective } from './structual-directive.directive';

describe('StructualDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StructualDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
