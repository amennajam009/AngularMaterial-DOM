import { AttributiveDirectiveDirective } from './attributive-directive.directive';

describe('AttributiveDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributiveDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
