import { StructuralDirectiveDirective } from './structual-directive.directive';

describe('StructualDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StructuralDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
