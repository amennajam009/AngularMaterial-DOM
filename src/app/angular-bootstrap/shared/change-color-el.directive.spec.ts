import { ChangeColorElDirective } from './change-color-el.directive';

describe('ChangeColorElDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeColorElDirective();
    expect(directive).toBeTruthy();
  });
});
